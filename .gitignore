node_modules/
dist/
*.tsbuildinfo
.vitest/
coverage/

# Smoke gate output (FORGE-S16-T11)
.smoke-out/
entelligentsia-forgecli-*.tgz

# Spike build artifacts (tsconfig.spike.json sets noEmit, but stale outputs
# can land here from earlier builds before the rootDir fix landed).
test/poc/**/*.js
test/poc/**/*.d.ts
test/poc/**/*.js.map

# Forge — transient agent event logs (one file per phase, do not commit)
.forge/store/events/

# Runtime residue when forge runs with this dir as cwd (smoke tests, --version)
.forge/
