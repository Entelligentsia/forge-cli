// Stage 1 scaffold (FORGE-S15-T01). Command registration shim.

import type { ExtensionAPI } from "@mariozechner/pi-coding-agent";

export function registerForgeCommands(_pi: ExtensionAPI): void {
	// intentionally empty — populated in T04 (forge_run_task spike)
}
