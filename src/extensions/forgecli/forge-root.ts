// Stage 1 scaffold (FORGE-S15-T01). Forge root resolver stub.
// Real impl reads `.forge/config.json` upward from cwd in a later task.

export function resolveForgeRoot(_cwd: string): string | null {
	return null;
}
