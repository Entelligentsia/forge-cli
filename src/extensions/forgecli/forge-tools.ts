// Stage 1 scaffold (FORGE-S15-T01). Tool registration shim.

import type { ExtensionAPI } from "@mariozechner/pi-coding-agent";

export function registerForgeTools(_pi: ExtensionAPI): void {
	// intentionally empty — populated in later S15 tasks
}
