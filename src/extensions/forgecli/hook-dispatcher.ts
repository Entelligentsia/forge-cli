// Stage 1 scaffold (FORGE-S15-T01). Hook dispatcher shim.

import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export function registerHookDispatcher(_pi: ExtensionAPI): void {
	// intentionally empty — populated in T06 (bash → custom-tool routing)
}
