// Stage 1 scaffold (FORGE-S15-T01). No registration yet.
// Real wiring lands in T03 (no-op extension) and T04+ (orchestrator spike).

import type { ExtensionAPI } from "@mariozechner/pi-coding-agent";

export default function forgecli(_pi: ExtensionAPI): void {
	// intentionally empty — see FORGE-S15-T03
}
