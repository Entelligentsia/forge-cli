# dogfood-fixture

Minimal Forge project used by `test/e2e/smoke.sh` to verify that `forge --help`
loads cleanly with `discoverForgeConfig()` finding `.forge/config.json`.

Not a real project. Do not extend — keep minimal.
